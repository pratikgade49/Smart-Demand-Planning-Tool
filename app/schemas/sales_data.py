"""
Sales Data schemas - currently not in use.

These schemas were planned for direct sales data CRUD endpoints that are not yet implemented.
Sales data is currently managed through the Excel upload endpoint, which handles bulk ingestion
and distribution to master data records.

Keeping this file as reference for future development of explicit sales data endpoints.
"""

# No active schemas - all sales data operations are handled through Excel upload and aggregation services
